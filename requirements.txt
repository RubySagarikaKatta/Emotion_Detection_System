# Core
opencv-python>=4.8.0
deepface>=0.0.79
streamlit>=1.32.0
plotly>=5.18.0
pandas>=2.0.0
numpy>=1.24.0

# DeepFace backend (TensorFlow is default; tf-keras needed for TF2.16+)
tensorflow>=2.13.0
tf-keras>=2.16.0

# Optional: faster face detection backend
# retina-face>=0.0.17

# Jetson / GPU note:
# On Jetson, install OpenCV with CUDA support via JetPack instead of pip.
# See README for Jetson-specific installation steps.
